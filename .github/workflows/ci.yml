name: CI
on: [push, pull_request]

# Required for Azure OIDC authentication
permissions:
  id-token: write
  contents: read
  packages: write

jobs:
  test:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_PASSWORD: testpass
          POSTGRES_DB: servicedesk_test
        ports: ["5432:5432"]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-dotnet@v4
        with: { dotnet-version: '8.0.x' }
      - run: dotnet restore
      - run: dotnet build --no-restore
      - run: dotnet test --no-build
      - run: dotnet list package --vulnerable --include-transitive

  build-and-push:
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/main'
    steps:
      - uses: actions/checkout@v4
      - name: Log in to the Container registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ghcr.io/${{ github.repository }}:${{ github.sha }}

  deploy-staging:
    runs-on: ubuntu-latest
    needs: build-and-push
    environment:
      name: staging
      url: ${{ steps.deploy.outputs.app-url }}
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}
      - name: Deploy to Azure Staging
        id: deploy
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            echo "Deploying to Staging..."
            az deployment group create --resource-group servicedesk-rg --template-file infra/main.bicep \
              --parameters deployApp=true appName=ca-servicedesk-api-staging imageName=ghcr.io/${{ github.repository }}:${{ github.sha }}
            
            APP_FQDN=$(az containerapp show -n ca-servicedesk-api-staging -g servicedesk-rg --query properties.configuration.ingress.fqdn -o tsv)
            echo "app-url=https://$APP_FQDN" >> $GITHUB_OUTPUT
            
      - name: Smoke Test
        run: |
          echo "Waiting for container to boot..."
          sleep 15
          curl -f ${{ steps.deploy.outputs.app-url }}/swagger/index.html

  deploy-production:
    runs-on: ubuntu-latest
    needs: deploy-staging
    environment:
      name: production
    steps:
      - uses: actions/checkout@v4
      - uses: azure/login@v2
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}
      - name: Deploy to Azure Production
        uses: azure/cli@v2
        with:
          azcliversion: latest
          inlineScript: |
            echo "Deploying exactly ${{ github.sha }} to Production..."
            az deployment group create --resource-group servicedesk-rg --template-file infra/main.bicep \
              --parameters deployApp=true appName=ca-servicedesk-api imageName=ghcr.io/${{ github.repository }}:${{ github.sha }}
